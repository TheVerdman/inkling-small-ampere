# Evaluation configurations

Evaluation configuration begins with a roughly 24-case smoke suite and expands
only after a reference oracle is captured. Prompts, assets, seeds, provider
metadata, and revisions will be versioned together.

