# Quantization manifests

Each quantization profile and conversion run will record exact inclusion rules,
scale layout, exclusions, source and output hashes, converter revision, and
per-tensor numerical statistics. No release profile exists before Gate A.

