# Raw run artifacts

Generated raw JSON reports and logs live here locally and are ignored by Git.
They are immutable: preserve them in the project artifact store and reference
their content-derived run IDs from committed status or report documents.

